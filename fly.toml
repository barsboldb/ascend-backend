app = "ascend-backend-barsbold"
primary_region = "sjc"

[build]

[[services]]
  internal_port = 8888
  protocol = "tcp"
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [[services.ports]]
    port = 50051
    handlers = []

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
